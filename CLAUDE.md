# CLAUDE.md — BriefManDraft 固有ルール

本プロジェクトは**共有コア参照方式**で運用する（検査エンジン・skill の正本は
`★AIの成果物/spec-driven-core/`）。共通ルールの正本は`C:\Users\user\.claude\CLAUDE.md`
（全プロジェクトのセッションで自動適用）。本ファイルは**固有ルールのみ**を最小構成で持ち、
共通ルールの写しは置かない（写しは同期ずれの温床。GROUP-32-1参照）。

- プロファイル：`.claude/spec-driven.config.yaml`（project / planning のみ上書き、他はコア継承）
- 台帳：`設計書類/06_プランログ.md`（受領 → Q → 確定 → 完了をここで回す）
- 初動手順：本ファイル＋`初動引き継ぎ_20260911.md`（`BriefMandraft/`直下・ワークスペースルート、過去のセッション記録が見つからないため実ファイル調査に基づき作成した代替引き継ぎ資料）
- 運用体制の正本：`★AIの成果物/デスクトップツール/ハウツー/18_Claude_運用体制.md`

## プロジェクト概要

- アプリ名：「依頼書びるだぁ」（`BriefMandraft.html`の`<title>`表記）。単一HTMLファイルで完結するツールで、AIへの機能追加依頼書を構造化して作成する用途と推定される（推定の根拠＝`開発依頼書/`配下のXML群の内容・命名。断定情報ではない）。
- GitHubリポジトリ：`https://github.com/javapanda2013/BriefManDraft`（remote `main`として設定済）。**git運用はこのプロジェクト単独で完結**（他の多くの★側プロジェクトと異なり、親フォルダのgit運用方針〔GROUP-4-1〕に依存しない）。
- 2026-09-11時点で最終コミットから約5.5ヶ月活動なし（dormant状態）。次セッション着手時は`初動引き継ぎ_20260911.md`§4の未消化検討事項（出力形式拡充・タグ機能・ワークフロー管理の3件が相談中）から優先順位をuserに確認する。

## ワークスペース構成（`.claude`とは別階層にあることに注意）

- `.claude/`・`設計書類/`・本CLAUDE.md・コード本体（`BriefMandraft.html`）は**`repo/`配下**（git追跡下）。
- `開発依頼書/`・`エクスポートデータ/`・`履歴/`・`開発ネタ.txt`・`初動引き継ぎ_20260911.md`は**`repo/`の一つ外側**（`BriefMandraft/`直下、git管理外）。両者を混同しない。

## 開発時の制約（`開発依頼書ビルダ_の機能調整.md`より継承・2026-03-28時点の記述）

このプロジェクト自身の依頼書サンプルに記載されていた制約。他プロジェクトとも共通する傾向のため踏襲する：

- ライブラリのバージョンを勝手に変更しない
- 既存コードの構造を無断で大幅変更しない
- 日本語コメントを英語に変換しない
- 使用技術の報告・型注釈必須・出力はコードブロック形式

## 共有コアの改修権限（Claude運用管理の専管）

- 共有コア（`spec-driven-core`）の改修は**Claude運用管理セッションの専管**。本プロジェクトから直接編集しない。
- `.claude/skills/spec-driven`は共有コアへの**シンボリックリンク（junction）**。編集すると全プロジェクトへ波及する。
- 改修が要る場合は依頼書（経緯／変更内容／仕様／検証実績／未決事項）をClaude運用管理へ提出する。

## 立ち上げ時の申し送り（2026-09-11導入時点・未整備・TODO）

- `source`/`trace`未設定（コア継承のためBorgesTagパスを指し`spec-cite --scope src`等は空振り）。単一HTMLファイルという性質上、必要性が低ければ設定不要。
- `release_gates`はコア既定（Firefox拡張前提のgateを含む）を継承中だが、本プロジェクトには当てはまらない項目が多い。運用開始後に要調整。
- `.claude/settings.local.json`にdangerous-command-guard／stop_hookのhookを配線済（2026-09-11、Claude運用管理が代理設定）。
- 事後発覚ミス台帳・Q_logは導入時点では空（ファイル自体は用意済み）。
- 08_ハウツー等プロジェクト個別の設計書類はまだ存在しない。必要に応じて追加する。

新たな注意事項・ルールは必ず本CLAUDE.mdに記載すること（口頭のみでは次セッションに引き継がれない）。
